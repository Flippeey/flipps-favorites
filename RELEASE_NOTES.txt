What's new in 2.6.1:

- Firefox icon loading restored — icons now load correctly on Firefox after a regression in 2.6.0 that blocked all icon sources due to a service-worker CORS restriction
- Firefox icon picker fixed — thumbnail images in the Edit Icon dialog now display correctly on Firefox
- Firefox manifest cleanup — removed redundant host-permission entries; the existing wildcard already covers all favicon services via XHR
